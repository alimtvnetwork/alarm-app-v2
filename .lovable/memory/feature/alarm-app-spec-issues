---
name: Alarm App Spec Issues Audit
description: 372 total spec issues — 315 resolved, 57 open (Discovery Phases 22-25). Missing deps, undefined interfaces, file structure gaps, IPC coverage holes, optional syntax inconsistency.
type: feature
---

## Audit Status — IN PROGRESS ⚠️

- **Total issues:** 372
- **Open:** 57 (Discovery Phases 22 + 23 + 24 + 25)
- **Resolved:** 315
- **Discovery phases:** 25
- **Fix phases:** 40 (Phases 1–20, A–T)

## Open Issues Summary

### Phase 22 — 18 issues (8 Critical, 7 Medium, 3 Low)
Missing deps (dnd-kit, reqwest, url, rand), missing group CRUD IPC, undefined interfaces (ImportPreview, WebhookPayload, RepeatPattern Rust), enum violations, stale counts.

### Phase 23 — 11 issues (2 Critical, 7 Medium, 2 Low)
AlarmOverlay hierarchy contradiction, PascalCase bugs in code samples (timerId, undoToken), missing command files (group.rs, challenge.rs), cheat sheet wrong column count.

### Phase 24 — 13 issues (1 Critical, 9 Medium, 3 Low)
File structure missing 5+ Rust command files for IPC domains (history, wellness, personalization), HistoryFilter `?` optional, font loading undefined, ClockState derivation gap, acceptance criteria raw strings.

### Phase 25 — 15 issues (2 Critical, 9 Medium, 4 Low)
AlarmEvent uses `?` optional on 5 fields (violates `| null` convention), `is_day_excluded` negative boolean in firing code, scheduling acceptance criteria raw strings, AlarmChallenge `?` optionals, AlarmSound has no storage mechanism, stale cheat sheet metrics, stale dates on 3 files.

## Key Finding — Phase 25

**P25-001 is systemic.** The `?` optional syntax vs `| null` convention is inconsistent across at least 3 interfaces (AlarmEvent, AlarmChallenge, HistoryFilter). A single sweep should fix all.

## Canonical Location

`spec/15-alarm-app/01-alarm-app-spec/14-spec-issues/`
